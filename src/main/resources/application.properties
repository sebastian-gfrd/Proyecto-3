# Application Name
spring.application.name=alpescab-microservices

# Server Configuration
server.port=8080

# MongoDB Reactive Configuration
# Configuración individual (comentada porque usamos URI)
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=alpescab_db
spring.data.mongodb.auto-index-creation=true

# MongoDB Authentication (descomenta y configura si tu MongoDB requiere autenticación)
# spring.data.mongodb.username=tu_usuario
# spring.data.mongodb.password=tu_password
# spring.data.mongodb.authentication-database=admin

# Opción alternativa: URI completa de MongoDB (incluye credenciales, host, puerto, base de datos)
# spring.data.mongodb.uri=mongodb://usuario:password@localhost:27017/alpescab_db?authSource=admin

# MongoDB Atlas (cloud) - CONFIGURACIÓN ACTIVA
spring.data.mongodb.uri=mongodb+srv://dbUser:jic!PMWYJ9U5ECK@cluster0.esextrf.mongodb.net/alpescab_db?retryWrites=true&w=majority

# Logging Configuration
logging.level.root=INFO
logging.level.com.alpescab=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Reactive Configuration
spring.reactor.debug-agent.enabled=true
